node_modules
.next
out
.DS_Store
*.log
.env*.local
.vercel
next-env.d.ts
dist-electron
*.exe
*.dmg
*.AppImage
*.deb
*.zip

*.tsbuildinfo

# Ed25519 license signing keys — private keys must NEVER be committed.
# Only the public key goes in the client (embedded as SP_LICENSE_PUBKEY_HEX).
*.pem
*.key
titan-private.*
keys/
